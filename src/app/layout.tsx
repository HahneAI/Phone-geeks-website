import type { Metadata } from "next";
import "./globals.css";
import { SiteHeader } from "@/components/layout/site-header";
import { SiteFooter } from "@/components/layout/site-footer";

// Resolves to the real Vercel deployment URL automatically; set
// NEXT_PUBLIC_SITE_URL once a custom domain is attached to override it.
const siteUrl = process.env.NEXT_PUBLIC_SITE_URL
  ? process.env.NEXT_PUBLIC_SITE_URL
  : process.env.VERCEL_URL
    ? `https://${process.env.VERCEL_URL}`
    : "http://localhost:3000";

export const metadata: Metadata = {
  metadataBase: new URL(siteUrl),
  title: "Phone Geeks | Phone, Computer & Tablet Repair in St. Louis",
  description:
    "Fast, honest phone, Macbook, computer, and tablet repair in the St. Louis area. One-hour turnaround, one-year warranty. Locations in Arnold and Ballwin, MO.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html lang="en" className="h-full antialiased">
      <body className="flex min-h-full flex-col font-sans">
        <SiteHeader />
        <main className="flex-1">{children}</main>
        <SiteFooter />
      </body>
    </html>
  );
}
